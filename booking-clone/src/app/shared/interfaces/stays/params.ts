export interface IStaysSearchParams {
  arrivalDate: string;
  departureDate: string;
  destId: string;
  searchType: string;
  page: number;
}

export interface IStayDetailsSearchParams {
  arrivalDate: string;
  departureDate: string;
  hotelId: string;
}
